<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Warm Ivory (brand). The adaptive icon uses the gradient PNG layer;
         this colour is the fallback for launchers that only read a colour. -->
    <color name="ic_launcher_background">#F5F1E9</color>
</resources>
